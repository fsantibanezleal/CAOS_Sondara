import { useMemo, useState } from 'react';
import './styles.css';

type Scenario = { id: string; name: string; holes: number; assays: number; grade: number; range: number; color: string };
const scenarios: Scenario[] = [
  { id: 'copper-ridge', name: 'Copper Ridge · porphyry', holes: 18, assays: 684, grade: 0.82, range: 142, color: '#f2b56b' },
  { id: 'north-shear', name: 'North Shear · structural', holes: 12, assays: 431, grade: 2.34, range: 88, color: '#e8839b' },
  { id: 'lithium-basin', name: 'Salar Edge · brine interface', holes: 24, assays: 912, grade: 1.17, range: 216, color: '#7ed7d0' },
];
const bands = [
  ['Oxide cap', 0, 24, '#283d4a'], ['Weathered halo', 24, 72, '#324b4f'],
  ['Potassic core', 72, 134, '#4a3f49'], ['Propylitic wall', 134, 198, '#2b4a50'], ['Fresh basement', 198, 260, '#243642'],
] as const;
const depthY = (depth: number) => 116 + depth * 1.62;

export default function App() {
  const [scenarioId, setScenarioId] = useState(scenarios[0].id);
  const [metric, setMetric] = useState<'grade' | 'uncertainty' | 'lithology'>('grade');
  const [method, setMethod] = useState('Ordinary kriging');
  const [selectedDepth, setSelectedDepth] = useState(132);
  const [playing, setPlaying] = useState(true);
  const [sources, setSources] = useState(['collar_survey.csv', 'assay_2024.csv', 'lithology_intervals.csv']);
  const scenario = scenarios.find((item) => item.id === scenarioId) ?? scenarios[0];
  const points = useMemo(() => Array.from({ length: 44 }, (_, i) => ({ depth: 8 + ((i * 29) % 242), grade: 0.12 + ((i * 17) % 100) / 38 })), []);
  const profile = useMemo(() => Array.from({ length: 15 }, (_, i) => ({ depth: i * 18 + 8, value: 0.22 + Math.abs(Math.sin(i * 0.71 + scenario.grade)) * scenario.grade * 1.55 })), [scenario]);
  const addFiles = (files: FileList | null) => files && setSources((current) => [...new Set([...current, ...Array.from(files).map((file) => file.name)])]);

  return <main className="sondara-shell">
    <header className="topbar"><div className="brand"><span className="brand-mark">◈</span><strong>SONDARA</strong><span className="brand-context">DRILLHOLE INTELLIGENCE</span></div><nav><button className="nav-active">Workbench</button><button>Data contract</button><button>Methods</button><button>Cases</button></nav><div className="top-actions"><span className="local-dot" /> LOCAL COMPUTE <button className="icon-button">◐</button></div></header>
    <section className="command-bar"><div><span className="eyebrow">PROJECT / OPEN PIT STUDY</span><h1>Subsurface signal, made inspectable.</h1><p>Load collars, surveys, assays and geology. Trace continuity, test support and compare estimators in one spatial model.</p></div><div className="command-controls"><label>Case<select value={scenarioId} onChange={(event) => setScenarioId(event.target.value)}>{scenarios.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><button className="primary-button">Run local estimate <span>↗</span></button></div></section>
    <section className="workspace-grid">
      <aside className="rail left-rail"><div className="rail-heading"><span>01</span><h2>Sources</h2><span className="check">✓</span></div><p className="rail-copy">Every observation keeps its support, unit and provenance.</p><label className="dropzone"><input type="file" multiple accept=".csv,.json,.parquet" onChange={(event) => addFiles(event.target.files)} /><span className="upload-icon">＋</span><strong>Drop source files</strong><small>CSV · JSON · Parquet</small></label><div className="source-list">{sources.map((file, index) => <div className="source-row" key={file}><span className="source-icon">{index === 0 ? '⌖' : index === 1 ? '∿' : '▦'}</span><span><strong>{file}</strong><small>{index === 0 ? '18 collars · projected CRS' : index === 1 ? '684 intervals · Cu %' : '5 lithologies · coded'}</small></span><b>✓</b></div>)}</div><button className="text-button">＋ Add source recipe</button><div className="rail-divider" /><div className="rail-heading compact"><span>02</span><h2>Support</h2></div><div className="support-card"><div><span>Composite length</span><strong>2.0 m</strong></div><div><span>Survey mode</span><strong>Minimum curvature</strong></div><div><span>Frame</span><strong>Local metric · metres</strong></div></div></aside>
      <section className="viz-column"><div className="viz-toolbar"><div><span className="eyebrow">SPATIAL MODEL / {scenario.name.toUpperCase()}</span><strong>Section explorer</strong><span className="toolbar-muted">{scenario.holes} holes · {scenario.assays} supports · Z 0–260 m</span></div><div className="toolbar-actions"><button className={playing ? 'toolbar-active' : ''} onClick={() => setPlaying((value) => !value)}>{playing ? 'Ⅱ Pause field' : '▶ Play field'}</button><button onClick={() => setSelectedDepth((depth) => depth === 132 ? 196 : 132)}>⌖ Follow cursor</button><button>⤢</button></div></div>
        <div className={`section-canvas ${playing ? 'is-playing' : ''}`}><div className="canvas-grid" /><div className="canvas-label top-left"><span>Y + 420</span><span>X + 180</span></div><div className="canvas-label bottom-right">LOCAL FRAME · EPSG:32719</div><svg className="section-svg" viewBox="0 0 920 580" role="img" aria-label="Animated three-dimensional drillhole section with assay points and grade continuity"><defs><linearGradient id="depthFade" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#101d29" /><stop offset="1" stopColor="#0c1821" /></linearGradient><filter id="glow"><feGaussianBlur stdDeviation="5" result="blur" /><feMerge><feMergeNode in="blur" /><feMergeNode in="SourceGraphic" /></feMerge></filter><linearGradient id="oreRibbon"><stop stopColor="#f2b56b" stopOpacity=".05" /><stop offset=".45" stopColor="#f2b56b" stopOpacity=".64" /><stop offset="1" stopColor="#e8839b" stopOpacity=".03" /></linearGradient></defs><rect x="42" y="52" width="836" height="478" rx="18" fill="url(#depthFade)" stroke="#284453" />{bands.map(([label, from, to, color]) => <g key={label}><rect x="62" y={depthY(from)} width="796" height={(to - from) * 1.62} fill={color} opacity=".44" /><text x="78" y={depthY(from + 13)} className="band-label">{label}</text></g>)}<path d="M210 112 C235 180 190 270 244 356 S205 464 223 518" fill="none" stroke="#6e8790" strokeWidth="7" opacity=".72" /><path d="M210 112 C235 180 190 270 244 356 S205 464 223 518" fill="none" stroke="#d6f1e5" strokeWidth="2" strokeDasharray="5 10" className="survey-line" /><path d="M485 112 C452 192 532 278 477 359 S510 466 486 518" fill="none" stroke="#6e8790" strokeWidth="7" opacity=".72" /><path d="M485 112 C452 192 532 278 477 359 S510 466 486 518" fill="none" stroke="#d6f1e5" strokeWidth="2" strokeDasharray="5 10" className="survey-line reverse" /><path d="M746 112 C716 188 780 270 724 354 S759 458 733 518" fill="none" stroke="#6e8790" strokeWidth="7" opacity=".72" /><path d="M746 112 C716 188 780 270 724 354 S759 458 733 518" fill="none" stroke="#d6f1e5" strokeWidth="2" strokeDasharray="5 10" className="survey-line" /><path d="M218 314 C325 260 407 292 486 318 S640 340 736 288" fill="none" stroke="url(#oreRibbon)" strokeWidth="34" filter="url(#glow)" className="ore-ribbon" /><path d="M218 314 C325 260 407 292 486 318 S640 340 736 288" fill="none" stroke="#f2b56b" strokeWidth="2" strokeDasharray="2 13" className="grade-front" />{[210, 485, 746].map((x, hole) => <g key={x}><circle cx={x} cy="112" r="10" fill="#d4e7e6" stroke={scenario.color} strokeWidth="4" /><text x={x - 20} y="91" className="hole-label">DH-{String(hole + 1).padStart(2, '0')}</text></g>)}{points.map((point, index) => { const x = [210, 485, 746][index % 3] + Math.sin(index * 1.4) * 18 + (index % 2 ? 5 : -5); const y = depthY(point.depth); const high = point.grade > 1.6; return <g key={index} className="assay-point" onClick={() => setSelectedDepth(point.depth)}><circle cx={x} cy={y} r={high ? 5.5 : 3.5} fill={high ? '#f2b56b' : '#67c8c1'} opacity=".96" /><circle cx={x} cy={y} r={high ? 12 : 7} fill="none" stroke={high ? '#f2b56b' : '#67c8c1'} opacity=".18" /></g>; })}<line x1="62" x2="858" y1={depthY(selectedDepth)} y2={depthY(selectedDepth)} stroke="#f2b56b" strokeWidth="1.5" strokeDasharray="7 7" /><rect x="72" y={depthY(selectedDepth) - 19} width="94" height="24" rx="12" fill="#f2b56b" /><text x="85" y={depthY(selectedDepth) - 3} className="cursor-label">{selectedDepth} m · {scenario.grade.toFixed(2)} %</text>{[0, 50, 100, 150, 200, 250].map((depth) => <g key={depth}><line x1="48" x2="58" y1={depthY(depth)} y2={depthY(depth)} stroke="#59717a" /><text x="18" y={depthY(depth) + 4} className="depth-label">{depth}</text></g>)}</svg><div className="viz-legend"><span><i className="legend-dot high" /> High grade</span><span><i className="legend-dot continuity" /> Continuity front</span><span><i className="legend-dot hole" /> Surveyed trace</span></div></div><div className="viz-footer"><span><b>CURSOR</b> {selectedDepth} m · DH-02 · 1.94 % Cu · support 2 m</span><span className="quality"><i /> spatial QA passed · 0 unresolved joins</span></div></section>
      <aside className="rail right-rail"><div className="rail-heading"><span>03</span><h2>Estimate</h2><span className="live-pill">LIVE</span></div><label>Method<select value={method} onChange={(event) => setMethod(event.target.value)}><option>Ordinary kriging</option><option>Simple kriging</option><option>Universal kriging</option><option>Indicator kriging</option><option>Sequential Gaussian simulation</option><option>Direct sampling (MPS)</option></select></label><div className="metric-switch">{(['grade', 'uncertainty', 'lithology'] as const).map((item) => <button key={item} className={metric === item ? 'active' : ''} onClick={() => setMetric(item)}>{item[0].toUpperCase() + item.slice(1)}</button>)}</div><div className="stat-grid"><div><span>Mean estimate</span><strong>{scenario.grade.toFixed(2)} %</strong><small>Cu equivalent</small></div><div><span>Spatial range</span><strong>{scenario.range} m</strong><small>major direction</small></div><div><span>Cross validation</span><strong>0.91</strong><small>R² · {scenario.assays} samples</small></div><div><span>P90 uncertainty</span><strong>±{(scenario.grade * .42).toFixed(2)}</strong><small>local support</small></div></div><div className="confidence"><div className="confidence-head"><span>Support confidence</span><strong>82%</strong></div><div className="confidence-bar"><i style={{ width: '82%' }} /></div><p>Dense collar spacing through the interpreted ore front. Uncertainty expands below 210 m.</p></div><div className="rail-divider" /><div className="rail-heading compact"><span>04</span><h2>Diagnostics</h2></div><div className="diagnostic-row"><span className="diag-ok">●</span><span>Coordinate frame</span><strong>OK</strong></div><div className="diagnostic-row"><span className="diag-ok">●</span><span>Support overlaps</span><strong>0</strong></div><div className="diagnostic-row"><span className="diag-warn">●</span><span>Extrapolation cells</span><strong>14%</strong></div><button className="outline-button">Open validation report ↗</button></aside>
    </section>
    <section className="analysis-strip"><div className="strip-title"><span className="eyebrow">ANALYSIS / {method.toUpperCase()}</span><h2>Does the signal travel between holes?</h2><p>Linked diagnostics update with the selected support and estimator.</p></div><div className="chart-card"><div className="chart-head"><span>Directional variogram</span><strong>{scenario.range} m range</strong></div><svg viewBox="0 0 330 104" className="mini-chart"><path d="M10 90 C38 18 75 26 106 45 S177 77 320 82" fill="none" stroke="#f2b56b" strokeWidth="3" /><path d="M10 92 L320 92" stroke="#38505a" /><path d="M10 20 L10 92" stroke="#38505a" /><circle cx="106" cy="45" r="5" fill="#f2b56b" className="chart-pulse" /><text x="102" y="102" className="axis-label">range</text><text x="12" y="16" className="axis-label">γ(h)</text></svg></div><div className="chart-card"><div className="chart-head"><span>Selected hole · DH-02</span><strong>grade profile</strong></div><svg viewBox="0 0 330 104" className="mini-chart"><path d={profile.map((item, index) => `${index ? 'L' : 'M'} ${18 + item.value * 70} ${10 + index * 6.2}`).join(' ')} fill="none" stroke="#7ed7d0" strokeWidth="3" /><path d="M18 8 L18 98 M18 98 L318 98" stroke="#38505a" /><text x="24" y="102" className="axis-label">0 m</text><text x="279" y="102" className="axis-label">260 m</text></svg></div><div className="decision-card"><span>MODEL DECISION</span><strong>Use OK for the 2 m composite</strong><p>Observed continuity supports a local estimate. Compare MPS below the interpreted contact before resource classification.</p><button className="text-button">View method notes ↗</button></div></section>
    <footer className="footer"><span>SONDARA · CAOS research project</span><span>Local-first · Apache-2.0 · no login</span><span>v0.1.0 · data never leaves this browser</span></footer>
  </main>;
}
